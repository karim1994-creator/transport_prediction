global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:

  - job_name: "transport-prediction-api"

    metrics_path: /metrics

    static_configs:

      - targets:
          - api:8000